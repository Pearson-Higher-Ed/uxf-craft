export interface Options extends ScrollIntoViewOptions {
    scrollMode?: 'always' | 'if-needed';
    boundary?: Element;
}
export declare const compute: (target: Element, options?: Options) => {
    el: Element;
    top: number;
    left: number;
}[];
